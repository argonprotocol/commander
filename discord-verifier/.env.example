# Secret and deployment settings. Supply the real token only in the untracked .env or host environment.
DISCORD_BOT_TOKEN=
ARGON_RPC_URL=wss://rpc.argon.network
DATABASE_PATH=/var/lib/argon-discord-verifier/verifier.sqlite
HOST=0.0.0.0
PORT=3000
CODE_TTL_MS=300000
